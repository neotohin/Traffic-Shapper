SQLite library for PERL:

yum -y install perl-DBD-SQLite



Set Variables as per need in  file "isplib/isptc.pm"

TO EXECUTE :

1. goto the script location e.g.  $ cd ~/bandwidthcontrol/
2. Then run bandwidthcontrol.pl   e.g  $ perl bandwidthcontrol.pl